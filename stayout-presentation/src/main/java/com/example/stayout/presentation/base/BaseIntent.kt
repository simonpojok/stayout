package com.example.stayout.presentation.base

/**
 * Marker interface for all user intents in the MVI pattern.
 *
 * An intent represents a user's desire to change something — a button tap, a text
 * input, a swipe gesture, or a lifecycle event. Intents are the only way the UI
 * layer communicates with the ViewModel; the ViewModel interprets each intent and
 * decides how to update state or emit events in response.
 */
interface BaseIntent
